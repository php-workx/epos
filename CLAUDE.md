# CLAUDE.md

Read `AGENTS.md` first. It is the durable repo instruction file.

For now, `IMPLEMENTATION_PLAN.md` is the authoritative design reference.

## Agent skills

### Issue tracker

Issues tracked with the `epos` CLI in this repo's own `.tickets/` store. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles mapped to default names as epos tags. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
